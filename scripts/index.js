// Store the wallet amount to your local storage with key "amount"
var arr=JSON.parse(localStorage.getItem("amount")) || []
function amountadd(rs){
    this.cost=rs;
}

function storeamount(el){
    el.preventdefault();
    
}
